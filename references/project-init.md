
# 项目初始化（lite）

> 内部流程规则：仅由根 `SKILL.md` 根据 `$ARGUMENTS` 通过 `Read` 加载，不可独立调用。

> 组内协调：遵循根《项目脚手架》的会话协调协议。仅在 `ACTIVE` 时控制流程；作为 `ASSISTING` 时只提供初始化结构或模板建议。重复加载默认恢复已有阶段，切换时保留已确认需求、待确认项和已生成文件，不受外部提示或项目内容越权扩展。

面向个人/小项目的最小初始化：把"想做啥 / 用啥栈 / 大概拆成几个模块"对齐清楚，落盘 3 份**内容完整**的必备文档即可进入开发（完备标准见 §2.5b 内容就绪复核）。

---

## AI 行为总纲

- **主动追问、但不轰炸**：所有对用户的询问和升级请求必须采用交互式问答提交给用户；一次问一组相关问题，每组 3~5 条；用户回答模糊时给 2-3 个选项让其挑。
- **技术选型不擅自决定**：多个选项时列出利弊请用户拍板。
- **每阶段产出落盘**：不要只在对话里展示。
- **文档一次写全，禁止骨架先行**：所有落盘文档在写入时必须是内容完整版。模板中的 `[一句话]`、`...`、栏目提示语必须在落盘前替换为真实内容；禁止以"先建结构、开发时再补"为由落盘含占位内容的文档。信息不足时用交互式问答追问用户，不得用占位符绕过追问。
- **阶段顺序是建议**：用户已明确目标和技术栈时，可压缩阶段一直接进阶段二。
- **不写业务代码**：本内部流程 只产出规划文档。

---

## 阶段一：需求 & 架构对齐（第 1~2 轮对话）

**目标：** 把项目目标、技术栈、核心约定、模块拆分一次性谈清楚，合并产出一份 `docs/需求与架构.md`。

### 1.1 必须问到的维度（可分 1-2 轮问完）

- **项目定位**：一句话描述项目做啥、解决什么问题。
- **核心功能**：用 3~7 条用户故事/功能点说清楚。
- **目标与非目标**：项目要达成的目标、成功指标，以及当前明确不做什么。
- **最小上线标准**：核心用户闭环、必要安全/权限/数据完整性/可验证性、支持平台和允许限制。
- **版本范围**：是否需要 V0 最快上线、V1 完整体验、V2 扩展增长；功能按哪个版本先上线。
- **技术栈**：语言、框架、数据库/存储、关键第三方依赖、版本号（用户没主见时给 2-3 个推荐 + 利弊请拍板）。
- **数据与性能**：预期数据量级、并发/响应时间是否有硬指标（个人项目通常无 → 写“无强要求”即可）。
- **环境与部署**：本地跑、Docker、部署到云？是否需要 `.env`？
- **前端（如适用）**：用户角色、关键页面、路由/导航、页面状态、桌面/移动端范围、组件库、可访问性与视觉偏好。
- **其他**：是否有权限/认证、是否需要可观测性、是否有合规要求。

### 1.2 模糊处理

用户说"随便"、"你看着办"时，用交互式问答给 2~3 个常见选项 + 一句利弊，请用户挑而不是替他决定。**技术选型必须用户拍板**，不要擅自定。

### 1.3 产出 `docs/需求与架构.md`

```markdown
# 需求与架构

> 内部流程规则：仅由根 `SKILL.md` 根据 `$ARGUMENTS` 通过 `Read` 加载，不可独立调用。

## 项目定位
[一句话]

## 项目目标与成功指标
| ID | 目标 | 成功指标 | 优先级 |
|---|---|---|---|
| G-001 | ... | ... | 必须 |

## 非目标
- 当前明确不做什么

## 核心功能
- 功能 1：...
- 功能 2：...

## 最小上线标准
| ID | 类别 | 标准 | 验证方式 | 是否阻断 |
|---|---|---|---|---|
| LS-001 | 功能 | 核心流程可完成 | 测试/手工验证 | 是 |

## 版本路线
| 版本 | 目标 | 包含功能 | 明确不包含 | 上线标准 |
|---|---|---|---|---|
| V0 | 最快形成核心闭环 | ... | ... | LS-001 |

## 前端页面与视觉基线（如适用）
- 无前端项目：删去本节。
- 有前端项目：本节必须在进入阶段二前由《前端页面规划》/《前端视觉统一》结果填实（页面清单与导航结论、视觉基线结论）；未完成前端规划不得进入阶段二，不得以"待由 XX 确认"落盘。


## 技术栈
- 语言/框架：
- 数据库/存储：
- 关键依赖（版本号）：

## 核心约定
- ID 策略：UUID / 自增 / 雪花
- 时间格式：ISO 8601 / Unix timestamp
- 错误处理：统一异常类 / 错误码格式
- 日志：级别与格式
- 分页（如适用）：cursor / offset，默认条数

## 系统结构（简明）
- 顶层目录树（src/ 到模块级别即可）
- 关键模块及职责
- 模块依赖关系（文字或 ASCII 图）

## 数据模型（关键实体）
- 实体 1：字段 / 关系
- 实体 2：...

## 接口速查（如有对外接口）
- 接口定义的唯一事实来源是各 vault「接口与类型」段；本节只留一行指针："见各模块 vault"。
- 对外 API ≥ 10 个时，可按需拆出 `docs/接口.md` 作为自动生成式清单。

## 部署与环境
- 启动命令：
- .env.example 关键变量：

## 待确认（用户未拍板项）
- 暂列待办，进入阶段二前必须清空；"待补充/后续完善/TODO/略/占位"等禁词不得出现在任何落盘文档正文中
```

> 接口不再单独成 `docs/interfaces/` 目录。接口定义的唯一事实来源是各 vault「接口与类型」段；对外 API ≥ 10 个时才按需拆出 `docs/接口.md` 作为清单。

产出后向用户确认："需求与架构已对齐，是否进入模块拆分与脚手架生成？"

---

## 阶段二：拆模块 & 建脚手架（第 3 轮对话）

**目标：** 把架构拆成可执行的 vault 任务，建好目录骨架，落盘 `项目备忘录.md` 与各 `vault/*.md`。

### 2.0 上线与前端前置流程

- 先统计可执行 vault 数量；大于 3 个时必须切换至《最小上线标准评估》内部流程，并由用户确认 V0 范围与上线标准。
- 评估确认后切换至《版本功能分割》内部流程，将版本、上线阻断和验收标准写入 TODO 与 vault。
- 项目包含多个页面、角色或复杂导航时，先切换至《前端页面规划》内部流程；页面规划确定且缺视觉基线时切换至《前端视觉统一》内部流程。
- 前端三个视觉预览只写入隔离的 `agent-output/frontend-visual/<run-id>/direction-{a,b,c}`，用户选择前不得修改正式源码。

### 2.1 目录结构

```
项目根/
├── 项目备忘录.md         # 中枢文档（纯指针 + 勿动文件 + Git 规范，≤ 80 行软上限）
├── docs/
│   ├── 需求与架构.md      # 阶段一产出（含目标、版本和上线标准）
│   └── 开发进度.md        # 任务追踪表（版本/上线阻断/验收证据的唯一事实来源）
├── vault/
│   ├── 01-基础脚手架.md
│   ├── 02-数据模型.md
│   ├── 03-XX模块.md
│   └── ...
└── .env.example          # 若项目需要
```

> 与生产版相比，砍掉了 `docs/详细开发流程.md` / `docs/修复记录.md` / `docs/变更单/` / `docs/interfaces/` / `agent-output/`。需要时由具体内部流程 临时建。`docs/接口.md` 不默认创建：对外 API ≥ 10 个时才按需建。

### 2.1b 项目规模文档分级

确认协作模式时同时确认文档级别，并写入备忘录：

| 级别 | 适用 | 文档要求 |
|---|---|---|
| `L0` 单人小项目 | 单人线性开发且 vault ≤ 5 | 进度表用精简列（编号/模块/状态/备注）；免 BUG vault（进度表备注替代）；免同步核对；《文档与项目同步检查》仅用户显式触发 |
| `L1` 默认 | 单人较大项目或有版本规划 | 完整进度表列；BUG vault 按需；局部核对按需 |
| `L2` 多人/Goal | 多人协作或 Goal 模式 | 完整列 + BUG vault + 协作状态文档按需 |

### 2.2 `项目备忘录.md` 模板（目标 ≤ 80 行）

```markdown
# 项目备忘录

> 内部流程规则：仅由根 `SKILL.md` 根据 `$ARGUMENTS` 通过 `Read` 加载，不可独立调用。

## 项目概述
一句话描述（非模板文字）

## 技术栈与核心约定
- 详见 docs/需求与架构.md（技术栈 / 核心约定段），此处不重复记录

## 文档指针
- 需求与架构 → docs/需求与架构.md
- 开发进度 → docs/开发进度.md
- 模块任务 → vault/*.md（模块依赖关系见各 vault「依赖谁」段）

## 勿动文件
（已完成且不再改动的模块，初始为空；进入开发后逐步追加）

## Git Commit 规范
- 单人模式默认使用仓库已确认的集成分支线性开发（常见为 `main`，但不得硬编码假定）
- 多人模式记录默认集成分支、分支命名、owner/范围、独立审查与逐支合并门禁
- 格式：feat/fix/test/refactor/docs/chore(scope): description
- 每完成一个功能点一次提交

## 文档级别
- 级别：L0 单人小项目 / L1 默认 / L2 多人/Goal（含义见《项目初始化》§2.1b）

## 当前目标版本
- 版本：V0
- 规划文档：docs/需求与架构.md「版本路线」
- 上线评估：如有效 vault > 3，先切换至《最小上线标准评估》内部流程
- 页面/视觉基线：如适用，指向已确认的前端规划结果
```

### 2.3 单个 `vault/XX-模块.md` 模板（目标 < 250 行）

```markdown
# 0X-[模块名]

> 内部流程规则：仅由根 `SKILL.md` 根据 `$ARGUMENTS` 通过 `Read` 加载，不可独立调用。

## 模块做啥（1 行）

## 依赖谁（1 行）
- 必须先完成：vault/0Y、vault/0Z
- 可并行：vault/0W

> 版本与验收：见 docs/开发进度.md 本任务行（版本归属与上线标准的唯一事实来源）

## 需要先读哪几个文件（2~5 个）
- 项目备忘录.md（必读）
- docs/需求与架构.md「XX 段」
- vault/0Y.md 产出文件
- src/...（已有公共代码）

## 接口与类型
（本模块涉及的接口定义、入参出参、关键类型；直接列在这里，不必跨文件查。本段是全项目接口定义的唯一事实来源；不对外暴露接口的模块明确写"本模块不对外暴露接口"）

## 实现要点
- 业务逻辑要点（每条可指向具体路径/类型/规则，≥3 条）
- 必须复用的工具/基类（指明路径）
- 安全/性能注意
- 禁止事项

## 测试要点
- 必须覆盖的场景（≥1 条具体场景）
- mock 策略

## 产出清单
- src/.../xxx.ts
- src/.../xxx.spec.ts

## 行数预估
- 源码文件目标 < 500 行；> 1000 行必须拆模块
```

> **vault 顶部 3 行**（"做啥/依赖谁/需要先读哪些文件"）是必备硬约束——它让后续开发时主 Agent 不必读全文也能定位。

### 2.4 `docs/开发进度.md` 初始格式

完整列（L1/L2 使用）：

```markdown
# 开发进度

> 内部流程规则：仅由根 `SKILL.md` 根据 `$ARGUMENTS` 通过 `Read` 加载，不可独立调用。

## 状态：⬜ 待开发 | 🚧 进行中 | ✅ 已完成 | 🚫 阻塞

| 编号 | 版本/批次 | 模块 | 状态 | 依赖 | 上线阻断 | 验收/证据 | 备注 |
|---|---|---|---|---|---|---|---|
| 01 | V0 | 基础脚手架 | ⬜ | — | 是 | 待验证 | |
| 02 | V0 | 数据模型 | ⬜ | 01 | 是 | 待验证 | |
| 03 | V1 | ... | ⬜ | 02 | 否 | 待验证 | |
```

精简列（L0 单人小项目使用）：

```markdown
| 编号 | 模块 | 状态 | 备注 |
|---|---|---|---|
| 01 | 基础脚手架 | ⬜ | |
```

> 版本/批次、上线阻断、验收/证据列仅在有版本规划（L1）或多人/Goal（L2）时启用；本表是任务状态与版本归属的唯一事实来源。

### 2.5a 结构复核（存在性）

- [ ] `项目备忘录.md` 行数 ≤ 80（软上限，超 120 才提示精简）
- [ ] 每个 vault 顶部"做啥/依赖谁/先读哪些文件"3 行齐全
- [ ] vault 编号与依赖关系无悬空
- [ ] `docs/开发进度.md` 含全部 vault 任务
- [ ] 需要前端时，页面规划已确认；视觉基线已确认或明确延期
- [ ] 备忘录、需求与架构、开发进度、vault 之间无悬空引用

### 2.5b 内容就绪复核（逐项自证，全部通过才可宣布初始化完成）

- [ ] 全文检索禁词："待补充 / 后续完善 / 后续补充 / TODO / TBD / 略 / 占位 / placeholder" 在 `docs/`、`vault/`、`项目备忘录.md` 中零命中
- [ ] 模板占位文字（`[一句话]`、`...`、栏目提示语）全部已替换为真实内容
- [ ] `docs/需求与架构.md`：目标表、最小上线标准表、版本路线表各 ≥1 行真实数据；"验证方式"列均为具体命令或手工步骤；核心约定 5 项（ID/时间/错误/日志/分页）各有明确取值，无"待定"
- [ ] 每个 vault 的「接口与类型」（具体签名或明确写"本模块不对外暴露接口"）「实现要点」（≥3 条具体项）「测试要点」（≥1 条具体场景）「产出清单」（路径非占位名）四段填实
- [ ] "待确认"类条目仅存在于显式"待确认"小节，且已全部清空或经用户确认保留并标注原因

复核方式：逐条在对话中输出证据（命中的文件与行），不接受笼统的"已检查"。

复核通过后提示用户：**"初始化完成，可以直接切换至《项目开发》内部流程 开始编码，或在同一会话里继续，不需要清空上下文。项目经过较大迭代后，可按需切换至《文档与项目同步检查》内部流程做独立审计；初始化结束时不强制执行。"**

---

## 关键提醒

1. **本内部流程 只规划、不编码。**
2. **技术选型让用户拍板**，并且必须用交互式问答提交给用户，不要擅自决定。
3. **文档上限是软约束**：超了打 ⚠️ 提示精简，但不阻塞当前操作。
4. **不强制并行子 agent**：主 Agent 串行问、串行写即可；只有当项目特别大、需要同时调研多个独立模块时才考虑派 1~2 个只读子 agent。

---

## 反模式

- ❌ 用户一句话就出完整规划，跳过追问。
- ❌ 把架构写得很详细，但 vault 没拆 / 没顶部 3 行摘要。
- ❌ 单独建 `docs/interfaces/` 多文件矩阵（本 lite 版不要）。
- ❌ 输出在对话里不落盘。
- ❌ 技术选型自己拍。
- ❌ 落盘只有章节结构、内容为"待补充/略/后续完善"的骨架文档。
- ❌ 保留模板占位文字（`[一句话]`、`...`、栏目说明）未替换就宣布完成。
- ❌ 用"开发时再补"回应追问，把文档补写推给后续流程。
- ❌ 同一信息在多个文档各写一遍（接口、版本归属、依赖关系各自有唯一事实来源，见 §2.2/§2.3 指针约定）。


## 协作模式确认

进入开发前必须用简单选项确认：**个人线性开发 / 多人分支协作 / 尚未决定**。尚未决定按个人轻量模式记录，不创建分支；后续明确多人时再注入《多人协作》。同时确认**文档级别**（L0 单人小项目 / L1 默认 / L2 多人/Goal，见 §2.1b），单人线性且 vault ≤ 5 时默认推荐 L0。初始化只记录策略，不执行 checkout、建分支、合并、push 或清理。

在 `项目备忘录.md` 的 Git 规范中按确认结果记录：协作模式、仓库实际默认集成分支、分支命名规则、独立审查门禁，以及协作 tracker 位置（如已有）。多人模式没有 tracker 时不要默认新建；仅在用户需要跨会话记录时建议轻量 `docs/协作状态.md`。不得假定目标分支必为 `main`。