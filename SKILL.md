---
name: "项目脚手架（个人项目 lite 版）"
description: "面向个人/小项目的轻量项目全生命周期 skill 套件入口。比生产版（poqi-oneshot-project-skill）大幅精简：默认主 Agent 自己干、不强制并行子 agent、不要求交接单落盘、不要求 3 轮反振荡计数。当用户提出与项目从零启动、迭代开发、需求变更、bug 修复、遗留接入相关请求但不确定调用哪个子 skill 时，先调本入口分发。"
---

# 项目脚手架 Skill 套件入口（lite）

本 skill 是一个**路由型入口**，本身不执行具体动作，只负责告知 AI 当前项目状态应该调用哪个子 skill。

> 这是「个人项目 lite 版」。相对生产版，去掉了多 agent 并行调度、交接单落盘、3 轮反振荡计数、强制阶段切换清空上下文等重机制；保留「先规划再编码」「编写→审查→测试→修复」「接口先行」「卡住就停下问人」等骨架价值。

---

## 子 skill 总览

| 子 skill | 适用阶段 | 核心职责 |
|---|---|---|
| `项目初始化` | 空白文件夹 → 规划完成 | 主动追问需求，生成最小脚手架：`项目备忘录.md`、`docs/需求与架构.md`、`vault/*.md` |
| `遗留项目接入` | 已有源码但缺脚手架 → 接入完成 | 从代码逆向归纳，补齐同款脚手架，让项目能被《项目开发》驱动 |
| `项目开发` | 规划完成 → 编码实现 | 主 Agent 串行驱动「编写→审查→测试→修复」循环，按 vault 任务推进 |
| `需求评估` | 变更/重构/bug 修复前 → 快速判断 | 快速评估可行性、影响范围、风险和后续去向，并把摘要传递给需求修改、重构协调、Bug修复或项目开发 |
| `需求修改` | 开发途中 → 需求变更 | 评估影响、批量同步文档，再回到《项目开发》继续 |
| `重构协调` | 开发途中 → 重构/改设计 | 协调《需求修改》和《项目开发》，先判断需求/设计/实现重构，再指引文档同步与编码返工 |
| `Bug修复` | 开发途中 → 明确 bug 修复 | 先分析问题并商讨方案，加入开发进度并生成 bug vault，再按复现→定位→修复→验证四步修复 |

---

## 路由规则（按目录状态判断）

### 调用 `项目初始化`
- 工作目录为空，或仅含 README/.gitignore 等极少初始文件。
- `项目备忘录.md`、`docs/`、`vault/` 都不存在。
- 用户表述："做一个 XX 项目"、"从零搭起"、"帮我初始化"。

### 调用 `遗留项目接入`
- 工作目录已有可观源码，且能识别依赖/构建清单。
- 但 `项目备忘录.md` / `docs/` / `vault/` 至少一项缺失。
- 用户表述："接入已有项目"、"给老项目补文档"、"逆向生成脚手架"。

### 调用 `项目开发`
- `项目备忘录.md` 已存在。
- `vault/` 下有可执行模块任务。
- 用户表述："继续开发"、"实现 XX 模块"、"按规划往下做"。

### 调用 `需求评估`
- 项目已进入开发或已有可运行代码。
- 用户提出新需求、需求调整、重构想法或 bug，但还没明确要求直接修改文档或代码。
- 用户表述："先评估一下"、"看看影响范围"、"是否可行"、"先判断方案"、"先分析 bug 修复方案"。
- 评估后必须把精简结论传递给《需求修改》《重构协调》《Bug修复》或《项目开发》，避免后续重复分析。

### 调用 `需求修改`
- 项目已进入开发（部分模块已完成或在做）。
- 用户提出新增/修改/删除功能、调整技术栈、变更性能要求等。
- 在动代码前需要先同步文档。
- 若用户只是想先判断可行性或影响范围，先调用 `需求评估`。

### 调用 `重构协调`
- 项目已进入开发，且用户提出"重构项目"、"修改现有设计"、"调整架构/模块边界/目录结构"、"推翻原方案"等。
- 需要先判断这是需求变更、设计重构还是实现重构。
- 需要协调《需求修改》先同步文档，再交给《项目开发》按返工 vault 编码。
- 若用户只是想先判断重构影响和是否值得做，先调用 `需求评估`。

### 调用 `Bug修复`
- 项目已进入开发或已有可运行代码。
- 用户报告运行时报错、测试失败、页面异常、接口返回不符合预期、数据错误等明确 bug。
- 修复前需要先分析问题、与用户商讨方案、写入 `docs/开发进度.md` 并生成对应 `vault/BUG-*.md`。
- 若用户只是想先评估根因方向或修复方案，先调用 `需求评估`。

---

## 主 Agent 行为约束（精简版）

1. **先判断阶段，再选 skill**：按上方目录状态判断当前阶段，再决定调用哪个子 skill。
2. **冲突时以用户显式指令为准**：用户明确指定调用哪个子 skill，则不再做阶段推断。
3. **阶段顺序是建议、不是强约束**：用户已经明确知道自己要做啥（如"用 React 写个计数器"）→ 可跳过详细需求追问，直接进开发规划；但开发途中明显需要先改需求时，应提示用户先走《需求修改》；明确报告 bug 时，应提示用户先走《Bug修复》。
4. **同一会话可串联**：初始化（或遗留接入）完成后用户立刻要开发，可直接串接《项目开发》，不强制清空上下文。
5. **询问与升级必须交互式提交**：所有对用户的询问、确认、拍板、卡住处理和升级请求，必须采用交互式问答提交给用户；需要选择时提供 2-3 个清晰选项，不用普通文本让用户自行复制回答。

---

## 子 skill 目录

```
./lite/
├── SKILL.md                # 本文件：路由入口
├── 项目初始化/SKILL.md      # 2 阶段：需求&架构对齐 → 拆模块&建脚手架
├── 遗留项目接入/SKILL.md    # 3 阶段：勘探 → 扫描归纳 → 补文档+复核
├── 项目开发/SKILL.md        # 主循环：编写→审查→测试→修复（卡 2 次就停下问人）
├── 需求评估/SKILL.md        # 快速判断：可行性 → 影响范围 → 后续子 skill 交接摘要
├── 需求修改/SKILL.md        # 3 阶段：澄清&评估 → 同步修改 → 确认
├── 重构协调/SKILL.md        # 协调：分类重构 → 影响清单 → 串联需求修改/项目开发
└── Bug修复/SKILL.md         # 3 阶段：问题分析&方案商讨 → 入计划+建 vault → 四步修复
```

---

## 提示词示例

- 触发 `项目初始化`："这个文件夹是空的，做一个 FastAPI 的 todo 后端，技术栈你来定，关键点问我。"
- 触发 `遗留项目接入`："这个仓库已经写了一半，没有 vault 也没备忘录，先接入流程再继续做。"
- 触发 `项目开发`："按 vault/03 实现用户认证模块。"
- 触发 `需求评估`："想加短信登录，先评估影响范围和是否可行，再决定要不要改需求。"
- 触发 `需求修改`："数据库从 SQLite 换成 PostgreSQL，先同步文档再动代码。"
- 触发 `重构协调`："现在的认证设计不合适，先帮我判断怎么重构，再决定改文档还是改代码。"
- 触发 `Bug修复`："登录接口 500，先分析原因并和我确认方案，再加入进度和 vault 后修复。"

---

## 常见误用

| 现象 | 正确做法 |
|---|---|
| 空目录被要求"实现登录" | 先《项目初始化》补齐最小脚手架，再进《项目开发》 |
| 老项目无脚手架被要求"继续开发" | 先《遗留项目接入》逆向补文档，再进《项目开发》 |
| 用户只想先判断新需求、重构或 bug 修复方案 | 先《需求评估》输出可行性、影响范围、风险和后续交接摘要 |
| 开发中用户提新功能，Agent 直接动代码 | 暂停编码，走《需求修改》同步文档后再回《项目开发》 |
| 用户说"重构/改设计"，Agent 不分类型直接改代码 | 先《重构协调》判断需求变更/设计重构/实现重构，再串联《需求修改》和《项目开发》 |
| 用户报告 bug，Agent 直接改代码 | 暂停编码，走《Bug修复》先分析和确认方案，再写开发进度与 bug vault |
| 用户只说"继续"无法判断阶段 | 报告当前 `项目备忘录.md`/`docs/`/`vault/` 状态，请用户确认下一步 |

---

## 能力边界

本套件覆盖：本地编码 → 自审 → 测试 → 提交。
**不覆盖**：CI/CD 流水线、生产部署、多仓库协同、业务取舍/预算/合同等非确定性决策（这类问题直接以交互式问答升级给用户）。
